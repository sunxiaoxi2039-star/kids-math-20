# MVP 产品框架设计

> 版本：v0.1 | 日期：2026-04-04
> 定位：面向内容创作者的对抗式多 Agent 协作平台

---

## 一、MVP 核心假设

> **"同一个命题，让多个 Agent 互相否定、找漏洞，最终逼出极致内容。"**

验证的核心问题：**对抗式精炼能否稳定产出比单 Agent 更好的内容？**

---

## 二、MVP 用户流程（极简版）

```
用户输入命题/主题
       ↓
选择对抗强度（轻度建议 / 中度辩论 / 激烈对抗）
       ↓
系统自动分配角色（无需用户配置）
       ↓
Agent 对抗过程（用户可观看，也可只看结果）
       ↓
裁判 Agent 综合最优方案输出
       ↓
用户接受 / 要求再来一轮
```

**设计原则：用户只做两件事 —— 输入命题、接受结果。**

---

## 三、角色设计（内容生产场景）

### 默认角色组（3 Agent 最小对抗单元）

| 角色 | 职责 | 风格 |
|------|------|------|
| **Writer**（写手） | 产出初稿，立场鲜明，敢于冒险 | 创意优先，不怕出格 |
| **Critic**（批评者） | 专门找漏洞、打脸、质疑每一句话 | 毒舌但有据可查 |
| **Editor**（编辑） | 裁判，综合双方，做出最优取舍 | 理性、以效果为准 |

### 对抗流程（一轮示例）

```
Writer  → 产出初稿
Critic  → 逐点批判（至少指出 3 个致命弱点）
Writer  → 针对批判修改，不可妥协，必须反驳或改进
Critic  → 二轮打分（是否真的改好了）
Editor  → 综合裁决，输出最终版本
```

### 可扩展角色（后期）

- **Devil's Advocate**（魔鬼代言人）：专门提出反直觉观点
- **Target Reader**（目标读者）：模拟真实受众的反应
- **Competitor**（竞品视角）：站在竞争对手角度挑毛病

---

## 四、技术架构（基于 Claude Agent SDK）

```
用户界面层（Web UI）
       ↓
编排层（Orchestrator）
  - 接收用户输入
  - 分配角色、设置对抗规则
  - 管理轮次和收敛条件
       ↓
Agent 执行层（Claude Agent SDK）
  - Writer Subagent（独立 context）
  - Critic Subagent（独立 context）
  - Editor Subagent（独立 context）
       ↓
共享状态层
  - 当前草稿版本
  - 批判记录
  - 轮次计数
```

**技术选型依据：**
- Claude Agent SDK：Subagent 独立 context + 并发支持，天然契合角色隔离
- Plan Approval 机制：Editor 可拒绝输出并要求再来一轮
- Hooks（TeammateIdle）：强制 Critic 必须完成批判才能进入下一步

---

## 五、对抗强度配置

| 强度 | 轮次 | Critic 风格 | 适用场景 |
|------|------|------------|---------|
| 轻度 | 1轮 | 建设性建议 | 日常内容，快速产出 |
| 中度 | 2轮 | 直接指出问题 | 重要文案，需要打磨 |
| 激烈 | 3轮+ | 不妥协式批判 | 核心品牌文案，追求极致 |

---

## 六、MVP 边界（不做什么）

- ❌ 不做视频生成（烧 token，竞争激烈）
- ❌ 不做复杂的用户角色配置界面（MVP 阶段固定 3 个角色）
- ❌ 不做多语言支持（先跑通中文内容场景）
- ❌ 不做企业级权限管理（那是下一阶段）
- ❌ 不做 Agent 之间实时可视化对话（先只展示过程摘要）

---

## 七、成功指标（如何判断 MVP 跑通了）

1. **质量验证**：同一命题，对抗版 vs 单 Agent 版，盲测用户更喜欢哪个（目标：>70% 选对抗版）
2. **效率验证**：3 轮对抗的总时间 < 用户自己来回修改 3 次的时间
3. **留存验证**：用过一次后，用户是否愿意再用第二次

---

## 八、第一个可演示的场景（Demo）

**场景：小红书/公众号爆款标题生成**

- 输入：一篇文章的核心观点（1-2 句话）
- 过程：Writer 出 5 个标题候选 → Critic 逐一找弱点 → Writer 改 → Editor 选出最强的 2 个
- 输出：2 个经过对抗精炼的标题 + 为什么这两个更好的理由

**为什么选这个作为 Demo：**
- 标题好坏有客观标准（点击率/吸引力），容易验证
- 用户能立刻感受到"对抗"带来的差异
- Token 消耗低，成本可控
- 内容创作者每天都有这个需求

---

## 九、下一步行动

- [ ] 用 Claude Agent SDK 实现最小对抗原型（Writer + Critic + Editor）
- [ ] 跑通标题生成 Demo，收集 10 个真实案例对比
- [ ] 设计最简 Web UI（输入框 + 强度选择 + 结果展示）
- [ ] 找 3-5 个内容创作者做第一轮用户测试
